title: "BotDeFi"
description: "Practical DeFi bot strategies, automation guides and on-chain insights."
baseurl: ""    # leave empty unless site is served from subpath
url: "https://botdefi.io"

markdown: kramdown

plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

# Collections, pagination, etc. (tùy chỉnh thêm nếu cần)
exclude:
  - Gemfile.lock
  - node_modules
